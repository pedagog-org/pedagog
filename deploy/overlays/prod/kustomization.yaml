apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - storage-class.yaml
  - metallb-pool.yaml
  - cluster-issuer.yaml
patches:
  - path: patches/postgres-storage.yaml
  - path: patches/registry-storage.yaml
