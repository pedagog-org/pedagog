apiVersion: v1
kind: Service
metadata:
  name: traefik-service
  namespace: pedagog-system
spec:
  type: LoadBalancer
  selector:
    app: traefik
  ports:
    - name: web
      port: 80
      targetPort: web
    - name: websecure
      port: 443
      targetPort: websecure
