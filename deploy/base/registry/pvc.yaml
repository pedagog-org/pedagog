apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-pvc
  namespace: pedagog-data
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: pedagog-storage
  resources:
    requests:
      storage: 1Gi  # patched per overlay
